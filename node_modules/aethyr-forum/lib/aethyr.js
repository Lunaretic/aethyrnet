// Default configuration.
var defaultOptions = {
  database : false,
};

module.exports = 
{
  //set up config.
  configure : function(options)
  {
    options = options || defaultOptions;
    
    if(options.database)
      this.database = options.database;
  },
  
  middleware : function aethyrMiddleware(req, res, next){

    console.log("Hello!");
    res.end("Hello!");
    //return next();
  }
}